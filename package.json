{
  "name": "roadie-agent",
  "description": "",
  "repository": "",
  "homepage": "",
  "author": "",
  "keywords": [],
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": ">=16"
  },
  "main": "./build/index.js",
  "files": [
    "build/lib/**/*",
    "build/index.*",
    "build/types/**/*"
  ],
  "scripts": {
    "test": "yarn build && mocha --inspect --require source-map-support/register --bail ./build/test/",
    "build": "rimraf build && tsc && tsc-alias",
    "preversion": "yarn lint && yarn build && yarn test",
    "version": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0 && yarn format && git add -A",
    "postversion": "git push --follow-tags && yarn publish",
    "lint": "eslint --ext ts,js --fix src",
    "format": "prettier src/**/*.ts -w",
    "build-live": "tsc -w && tsc-alias -w"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "node-fetch": "^2.6.7",
    "snyk-broker": "^4.137.9",
    "@backstage/plugin-catalog-backend": "^1.5.1",
    "@backstage/plugin-catalog-node": "^1.2.1"

  },
  "devDependencies": {
    "@types/chai": "^4.2.18",
    "@types/fs-extra": "^9.0.11",
    "@types/mocha": "^8.2.2",
    "@types/node": "^15.6.0",
    "@types/node-fetch": "^2.6.2",
    "@typescript-eslint/eslint-plugin": "^4.25.0",
    "@typescript-eslint/parser": "^4.25.0",
    "chai": "^4.3.4",
    "conventional-changelog-cli": "^2.1.1",
    "eslint": "^7.27.0",
    "eslint-plugin-prettier": "^3.4.0",
    "fs-extra": "^10.0.0",
    "mocha": "^8.4.0",
    "prettier": "^2.3.0",
    "rimraf": "^3.0.2",
    "source-map-support": "^0.5.19",
    "tsc-alias": "^1.2.11",
    "typescript": "next"
  }
}
